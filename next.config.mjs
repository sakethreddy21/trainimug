/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['cdn.sanity.io', 'media.licdn.com', 'aceternity.com', 'images.rawpixel.com', 'via.placeholder.com', 'www.aceternity', 'picsum.photos'],
  },
 
};

export default nextConfig;
